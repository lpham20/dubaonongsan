[tool.ruff]
line-length = 120
target-version = "py310"
extend-exclude = [
  "frontend/node_modules",
  "frontend/dist",
  "artifacts",
  "training_snapshots",
]

[tool.ruff.lint]
select = ["E", "F", "I", "B", "UP"]
ignore = ["E501"]
